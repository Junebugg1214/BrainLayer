[project]
name = "brainlayer"
version = "0.1.0"
description = "Minimal BrainLayer prototype with JSON schemas and a tiny benchmark harness."
readme = "README.md"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
